# CI / development dependencies (Phase 22).
#
# Mirrors `pyproject.toml [project].dependencies` + `[project.optional-dependencies].dev`
# plus the runtime extras the test suite and migration runner import directly
# (python-dotenv, websockets). Installed by CI with:
#
#     pip install -r requirements-dev.txt
#
# Do not upgrade versions here without a real CI/security blocker — the
# repository pins are the single source of truth.
SQLAlchemy>=2.0.52
Alembic>=1.19.1
FastAPI>=0.141.1
psycopg[binary]>=3.2
uvicorn>=0.30
pydantic-settings>=2.0
argon2-cffi>=23
pytest>=9.0.0
httpx>=0.28.1
ruff>=0.6
python-dotenv>=1.0
websockets>=12
